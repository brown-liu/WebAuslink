@model IEnumerable<WebAuslink.Models.DailyToDoList>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.today)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MorningJob)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AfternoonJob)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ExtraWork)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.today)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MorningJob)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AfternoonJob)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExtraWork)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
